package Java.Data_Structures.Linked_List.Singlly;
import java.util.*;
public class Test {
    /*public static void main(String ardes[]) {
        StringBuilder s = new StringBuilder("AA");
        StringBuilder ss = new StringBuilder("XXXXXXXXXXXXXXXXXXXXXXXXXX");
        int k=0,n=0;
        n = ss.length()>s.length()?ss.length():s.length();
        for (int i = 0; i < n; i++) {
            s.insert(k + 1, ss.substring(i, i + 1));
            k=k+2;
            System.out.println(s.toString());
        }
        }
*/
// write your code here


}